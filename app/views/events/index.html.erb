<section class="heroBlock">
  <div class="flex flex-col items-center justify-around">
    <div class="max-w-4xl px-4 pt-24">
      <h1 class="text-5xl md:text-6xl font-bold text-center">Ближайшее мероприятие</h1>
    </div>

    <div class="w-full py-10 px-16 md:py-18 md:px-52 flex justify-center items-center">
      <%= image_tag('logo-textless.svg', class: 'h-full w-full max-w-sm', alt: 'Trampoline Logo') %>
    </div>
  </div>
</section>

<section class="flex justify-center items-center mx-auto max-w-7xl mb-6 sm:mb-10 py-10 px-8 bg-white rounded-lg shadow-sm">
  <div class="flex flex-col flex-1 w-full items-center justify-center">
    <div class="max-w-4xl px-4 mb-7">
      <h2 class="text-5xl md:text-6xl font-bold text-center">Прошедшие мероприятие</h2>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6 w-full">
      <%= render EventCard::Component.new %>
      <%= render EventCard::Component.new %>
      <%= render EventCard::Component.new %>
      <%= render EventCard::Component.new %>
      <%= render EventCard::Component.new %>
      <%= render EventCard::Component.new %>
    </div>
  </div>
</section>

<%= render Blocks::FreeSharing::Component.new %>
